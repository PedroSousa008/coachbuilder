/**
 * Quando existirem URLs de vídeo por dia, devolver aqui o URL para download / embed.
 * Por agora null → a UI oferece um ficheiro informativo em alternativa.
 */
export function getLessonVideoUrl(_dayKey: string): string | null {
  return null;
}
